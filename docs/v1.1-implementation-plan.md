# LocalSpace v1.1 编程能力与执行效率增强实施计划

## 1. 文档状态

| 项目 | 当前值 |
| --- | --- |
| 基线版本 | `v1.0.6` |
| 基线提交 | `1a4b0c2 chore: bump version to 1.0.6` |
| 当前分支 | `main` |
| 路线名称 | LocalSpace v1.1 编程能力与执行效率增强路线 |
| 当前边界 | E1/E2/E2.5/E3/E4/E5/E6 implementation and automated validation complete；real ChatGPT acceptance pending；E7 evaluation next |
| 文档性质 | 正式实施计划；不代表所列能力已经实现或受支持 |

本计划用于指导 LocalSpace 在现有稳定架构上增强 ChatGPT 编程效率、工具契约一致性、性能可观测性、受控并发和后续高级代码能力。

任何阶段只有在代码、测试、文档和真实 MCP 客户端验收一致后，才能描述为 `supported`。仅完成设计、本地实现或自动化测试时，必须准确注明实际边界。

---

## 2. 背景与当前判断

LocalSpace 当前已经具备完整的常规单仓库编程闭环：

- 工作区和受控根目录；
- 项目指令与 Agent Skills；
- 文件读取、搜索和目录导航；
- TypeScript/JavaScript 轻量符号、导入和引用扫描；
- Codex 风格补丁修改；
- 长进程、PTY 和进程轮询；
- Git 状态、Diff、Stage、Commit 和 Log；
- aggregate changes 展示；
- OAuth、敏感路径保护、高风险命令审批和审计日志；
- checkout 和 managed worktree 模式；
- stateless MCP 重启恢复。

当前主要差距不在于缺少基础文件工具，而在于：

1. 实际注册工具、服务器指令、文档和 Skill 存在漂移；
2. 尚不能准确区分 ChatGPT、网络、MCP 固定成本和工具执行成本；
3. 多个已知文件仍需多次 MCP 往返读取；
4. 多项独立验证仍需串行启动和轮询；
5. LocalSpace 服务端没有显式的有界并发调度和状态锁；
6. 大型重构缺少完整语言服务语义；
7. 前端任务缺少浏览器反馈闭环；
8. 项目级权限策略和确定性自动化仍较有限。

本路线优先解决正确性、可观测性和往返效率，不以扩大默认工具数量为目标。

---

## 3. 产品目标

LocalSpace 应继续保持以下定位：

> ChatGPT 或其他 MCP 主机直接、显式、安全地调用本地编程工具，而不是在本地运行一个不可见的自治 coding agent。

本路线的目标是：

- 使常规单仓库开发效率进一步接近 Codex 和 Claude Code；
- 减少无效工具选择和不必要的 MCP 往返；
- 对读、写、Git 和进程工具建立确定性的并发语义；
- 保持默认 `hybrid` 工具面有界；
- 将高级能力按可选工具包渐进披露；
- 保持所有写操作、安全审批和用户可见 Diff 可审查；
- 为性能优化建立可重复的基准，而不是凭感觉判断；
- 保证旧配置和既有核心工作流不因增强路线被无意破坏。

---

## 4. 非目标

本路线暂不承诺：

- 完整复制 Codex CLI 或 Claude Code；
- 默认启用自治 subagent 或 agent team；
- 允许多个代理同时修改同一个 checkout；
- 提供无边界的通用 `parallel_tools` 元工具；
- 默认运行仓库提供的任意 Shell Hook；
- 默认允许浏览器访问任意外部网站；
- 一次性接入所有编程语言的 LSP；
- 为追求理论延迟恢复不稳定的 stateful MCP 默认模式；
- 让项目级配置扩大服务器 allowed roots 或绕过敏感路径保护。

---

## 5. 核心设计原则

### 5.1 一致性先于新增能力

以下五层必须保持一致：

```text
实际注册工具
= server instructions
= 文档
= Skill 指导
= 测试合同
```

发现漂移时，应先修复真实合同，再添加新工具。

### 5.2 优先减少往返，不堆积微型工具

ChatGPT 通过 LocalSpace 编程时，主要交互链路通常是：

```text
模型决策
→ MCP 请求
→ 网络、OAuth 与 Transport
→ LocalSpace 工具执行
→ 返回结果
→ 模型再次决策
```

因此，应优先建设：

- 批量读取；
- 受控并行验证；
- 紧凑结构化输出；
- 准确 Tool Search；
- 合理的组合工具；
- 可测量的请求和工具指标。

### 5.3 安全操作尽可能并行，有状态操作确定性串行

并行能力不应被理解为“所有工具都同时运行”。默认规则是：

- 独立只读操作可以并行；
- CPU 重型扫描有界并行；
- 不同进程 Session 可以并行；
- 同一进程 Session 的交互必须串行；
- 同一工作区的修改操作必须串行；
- 同一 Git Root 的 Git 写操作和 Review Checkpoint 必须串行。

### 5.4 默认核心与可选工具包分离

默认核心继续覆盖：

- workspace；
- 文件读取和搜索；
- 轻量代码导航；
- patch 编辑；
- 进程；
- Git；
- Diff；
- 基础诊断。

后续高级能力优先作为可选工具包：

- `code-intelligence`；
- `browser`；
- `advanced-policy`；
- `experimental-collaboration`。

### 5.5 项目策略只能收紧权限

项目级策略可以禁止或限制操作，但不能：

- 扩大服务器 allowed roots；
- 绕过敏感路径保护；
- 自动信任仓库提供的任意代码；
- 降低命令风险等级；
- 绕过用户审批。

### 5.6 每个阶段独立验收

每个实现阶段至少执行：

```text
focused tests
→ full tests
→ typecheck
→ build
→ docs review
→ real ChatGPT/MCP acceptance
→ git status verification
```

真实客户端验收未完成时，不得将客户端行为描述为已支持。

---

## 6. 路线总览

| 阶段 | 主要内容 | 优先级 | 建议版本边界 |
| --- | --- | --- | --- |
| E0 | 基线、计划与验收协议 | P0 | 不单独发布 |
| E1 | 工具契约与文档一致性 | P0 | `v1.0.7` |
| E2 | 性能、活动与请求可观测性 | P0 | `v1.0.7` |
| E2.5 | 有界工具并发基础设施 | P0/P1 | `v1.0.7` 或 `v1.1.0` 前置 |
| E3 | `read_many` 批量读取 | P1 | `v1.1.0` |
| E4 | `run_checks` 受控并行验证 | P1 | `v1.1.0` |
| E5 | Debug、Review、Refactor Skills | P1 | `v1.1.0` |
| E6 | TypeScript/JavaScript 语义代码能力 | P2 | `v1.2.0` |
| E7 | 浏览器调试能力评估 | P2 | 待评估 |
| E8 | 权限策略与确定性自动化 | P2/P3 | 待评估 |
| E9 | Subagent 研究 | 暂缓 | 不承诺版本 |

推荐顺序：

```text
E0
→ E1
→ E2
→ E2.5
→ v1.0.7 acceptance
→ E3
→ E4
→ E5
→ v1.1.0 acceptance
→ E6
→ v1.2.0 acceptance
→ E7
→ E8
→ E9 decision
```

---

## 7. E0 — Baseline, Plan, and Acceptance Contract

### 7.1 目标

冻结 `v1.0.6` 的真实工具面、性能和客户端行为，形成后续优化的统一比较基线。

### 7.2 E0-T01：正式计划文档

本文件即为本任务产物。后续实施中需要持续更新：

- 阶段状态；
- 验收结果；
- 被否决设计；
- 版本边界；
- 已知风险。

### 7.3 E0-T02：工具面基线

记录并测试各模式准确工具列表：

- `minimal`；
- `full`；
- `codex`；
- `hybrid`。

不应长期维护多份手写列表。工具目录、测试和文档应尽量由单一元数据来源生成或校验。

当前状态：完成。`docs/baselines/v1.0.6-tool-surfaces.json` 冻结了四种工具模式在 Widgets 关闭时的运行时 `tools/list`，并单独记录 `changes` 与 `full` Widget 覆盖。`src/mcp-server-restart.test.ts` 会启动真实 stateless MCP 服务验证该合同。

### 7.4 E0-T03：真实调用基线

固定代表性任务：

1. 打开项目并获取状态；
2. 查找函数定义及引用；
3. 读取三个相关文件；
4. 修改一个文件并检查 Diff；
5. 执行 focused test；
6. 执行完整测试；
7. 同时启动两个独立进程；
8. LocalSpace 重启后在旧 ChatGPT 对话继续调用。

记录：

- MCP 调用次数；
- 总体交互轮次；
- LocalSpace 工具执行时间；
- 请求总耗时；
- 输出字符数；
- 截断情况；
- 成功、失败或 blocked；
- 是否出现无效工具选择；
- ChatGPT 是否原生并行发起独立 MCP 调用。

当前状态：完成。`scripts/benchmark-mcp.mjs` 可创建临时 Git Workspace 和临时 OAuth 状态，启动真实 stateless hybrid MCP 服务并生成客户端观察到的 Loopback 基准。当前基线保存在 `docs/baselines/v1.0.6-loopback-benchmark.json`。

### 7.5 E0-T04：验收场景冻结

后续版本必须复用相同场景进行前后对比。需要新场景时可以增加，但不得删除不利结果来改善表面指标。

当前状态：完成。`docs/baselines/v1.0.6-acceptance.md` 冻结了自动化场景、真实 ChatGPT 场景、当前证据状态和后续版本比较规则。

### 7.6 E0 验收

- 正式实施计划存在；
- 各模式工具面有基线；
- 有可重复性能基线；
- 有真实 ChatGPT/MCP 验收清单；
- 未提前实现后续阶段能力。

当前状态：完成。工具面运行时合同、Loopback 基准和真实 ChatGPT/MCP 验收协议均已建立；`npm test`、`npm run typecheck` 和 `npm run build` 通过。Build 仅保留既有的大 Chunk 和插件耗时警告，没有新增失败。

---

## 8. E1 — Tool Contract and Documentation Alignment

### 8.1 目标

消除工具注册、服务器指令、文档、Skill 和测试之间的漂移。

### 8.2 E1-T01：建立单一工具目录

建议新增：

```text
src/tool-catalog.ts
```

候选结构：

```ts
interface ToolCatalogEntry {
  name: string;
  modes: ToolMode[];
  category: ToolCategory;
  summary: string;
  workflowHint?: string;
  concurrencyClass: ToolConcurrencyClass;
}
```

工具目录只负责可信元数据，不负责工具执行。它应成为以下内容的依据：

- 工具名称；
- 模式可用性；
- 精简描述；
- 工具分类；
- 并发分类；
- instructions 和文档校验。

当前状态：已实现。`src/tool-catalog.ts` 现在集中维护工具名称、模式、分类、并发分类和精简描述，并可生成精确工具面文档。

### 8.3 E1-T02：修复 hybrid instructions

当前 `hybrid` 服务器指令引用了实际未在该模式注册的 workflow helper 工具。默认方案是：

- 不将所有 helper 工具重新加入默认 `hybrid`；
- `hybrid` instructions 只引用实际注册工具；
- 工作流方法继续由 Skill 渐进披露；
- 旧模式保留既有 helper 工具以维持兼容性。

当前状态：已实现并通过合同测试。服务器 instructions 根据当前模式和 Widget 配置生成，只引用实际可用工具。后续接口审查确认 Hybrid 在任务收尾阶段仍缺少必要汇总能力，因此已选择性加入 `next_steps`、`validation_summary`、`final_report` 和 `handoff_summary`；`validate_plan`、`review_checklist` 与 `task_summary` 继续保留在兼容工具面，避免无边界扩大默认工具数量。

### 8.4 E1-T03：修正文档

至少审查：

- `README.md`；
- `docs/chatgpt-coding-workflow.md`；
- `docs/configuration.md`；
- `docs/structured-content.md`；
- 内置 Skill。

要求：

- 各模式工具列表准确；
- workflow helper 的真实模式边界明确；
- Skill 明确属于指导层，不是动态工具注册层；
- 不描述未验收能力为 supported。

当前状态：已完成。README 和相关 docs 已明确 default `hybrid`/experimental `codex` 与 legacy helper 的真实边界，精确工具列表由 `docs/tool-surfaces.md` 提供。

### 8.5 E1-T04：工具面合同测试

新增或扩展测试，精确断言：

- 各模式必须存在的工具；
- 各模式不应存在的工具；
- instructions 中提到的工具均已注册；
- 工具目录与注册结果一致；
- 文档中的机器可校验片段不发生漂移。

当前状态：已实现。目录合同、生成文档合同、instructions 可用性合同和真实 stateless MCP `tools/list`/description 合同均已覆盖。

### 8.6 E1-T05：精简工具描述

公共前置要求放入服务器 instructions，单个工具描述集中说明自身职责。避免每个工具重复完整的 `open_workspace` 使用规则，改善 Tool Search 选择性。

当前状态：已实现。工具描述全部来自工具目录，不再重复 `Call open_workspace first`；公共工作流要求保留在服务器 instructions。

### 8.7 E1 验收

- 所有模式工具面合同测试通过；
- instructions 不引用不可用工具；
- 文档与实际工具一致；
- Tool Search 查询不再因重复文本无意义匹配大部分工具；
- full tests、typecheck、build 通过；
- 真实 ChatGPT 重新连接后工具列表正确。

当前状态：前五项自动化边界已通过。`npm test`、`npm run typecheck` 和 `npm run build` 成功；Build 仅有既有的大 Chunk 警告。最后一项需要正在运行的 LocalSpace 服务加载新构建并由真实 ChatGPT 重新发现工具后确认，因此 E1 尚未宣告完整验收，E2 尚未进入。

---

## 9. E2 — Performance, Activity, and Request Observability

### 9.1 目标

回答“慢在哪里”，并为并发和批量能力建立可信基线。

### 9.2 E2-T01：MCP 请求阶段耗时

当前状态：实现与自动化验收完成；真实 ChatGPT 验收随服务重启统一补做。

当前实现记录：

- OAuth authentication；
- stateless/stateful MCP Server 构造；
- Transport connect；
- Transport handle；
- stateless cleanup；
- Server total time；
- HTTP status、request/response bytes、RPC method、tool 和 workspace。

由于当前 MCP SDK 不分别暴露 Tool dispatch 与 JSON serialization，
`transportHandleMs` 暂时包含 SDK dispatch、工具执行和响应序列化；工具自身
执行时间继续由 Activity 指标提供。该边界必须在后续报告中保持明确。

记录或聚合以下阶段：

```text
auth
server_create
tool_registration
transport_connect
tool_execution
response_serialization
cleanup
total_request
```

stateless 模式尤其需要区分：

- 每次创建 MCP Server 的固定成本；
- 工具自身成本；
- OAuth 和 HTTP 成本；
- 输出序列化成本。

### 9.3 E2-T02：统一工具指标

当前状态：实现与自动化验收完成；真实 ChatGPT 验收随服务重启统一补做。

已实现：

- success/failure、duration、running、exit code、truncated；
- workspace、tool category、concurrency class；
- process output bytes；
- request/response HTTP bytes；
- 所有注册工具返回的 `content` 与 `structuredContent` UTF-8 字节数；
- queued time 字段和聚合结构已预留，等待 E2.5 调度器接入。

输出指标只记录字节数，不为了指标保留实际文本、图像数据或 structured
payload。queued time 在 E2.5 调度器接入前保持 0，这是明确的阶段边界。

工具指标至少包含：

- 成功、失败、blocked；
- queued time；
- execution time；
- running；
- output size；
- structured output size；
- truncated；
- workspace；
- tool category；
- concurrency class。

不得默认记录完整文件内容或敏感命令。

### 9.4 E2-T03：活动记录与持久安全审计分层

当前状态：实现与自动化验收完成；真实 ChatGPT 验收随服务重启统一补做。

实现边界：

- 新增有界 `ToolActivityLogManager`，记录所有近期工具活动；
- 活动记录按可信工具目录补充 category 和 concurrency class；
- 普通 read/search/navigation 活动只进入内存，不写入 `audit.jsonl`；
- 现有文件修改、Shell、Git 写、安全审批和 blocked 事件继续使用 Durable Audit Log；
- 活动环容量暂复用 `LOCALSPACE_AUDIT_MAX_MEMORY_EVENTS`，不新增配置项。

建议分为：

#### 内存 Activity Ring Buffer

- 记录所有近期工具；
- 用于 `session_summary`；
- 有界保留；
- 服务重启后允许丢失。

#### Durable Audit Log

- 文件修改；
- Shell 命令；
- Git 写操作；
- blocked 和 approved；
- 安全风险；
- 关键认证和生命周期事件。

这样可以提高 `session_summary` 完整性，同时避免普通读取造成持久日志快速增长。

### 9.5 E2-T04：扩展 `session_summary`

当前状态：实现与自动化验收完成；真实 ChatGPT 验收随服务重启统一补做。

当前实现增加：

- 所有近期工具调用和按工具聚合；
- category 与 concurrency class 聚合；
- 成功、失败、running、truncated 和 process poll 计数；
- 平均/最大执行时长；
- 预留 queued time 聚合字段，E2.5 调度器接入后填充；
- Durable Audit 事件数、blocked、approved、commands、risks 和近期安全事件。
- Workspace 级 MCP request count、RPC/tool 分布、request/response bytes，以及
  auth/server-create/connect/handle/cleanup/total 分段耗时。

建议增加：

- 所有近期工具调用；
- 每工具调用次数；
- 成功率；
- 平均和最大耗时；
- 排队耗时；
- 截断次数；
- 进程轮询次数；
- 读、写、Git 和进程分类。

优先扩展现有工具，不新增默认 `performance_summary`。

### 9.6 E2-T05：基准脚本

当前状态：实现与自动化验收完成；基准 Schema 已升级到 v2。

当前脚本除客户端 Loopback 时间外，还通过 `session_summary` 输出服务端观察到
的 request phase 聚合。2026-07-21 的本机开发态样本显示：OAuth 与 Transport
connect 开销很小，stateless Server 构造约为数毫秒；长进程轮询的主要时间位于
`transportHandle`。该样本不包含 ChatGPT 模型延迟、公网 Tunnel 或浏览器渲染。

建议新增：

```text
scripts/benchmark-mcp.mjs
```

覆盖：

- loopback；
- 可选公网 URL；
- initialize；
- `tools/list`；
- 并发 `tools/list`；
- `open_workspace`；
- `read`；
- `grep`；
- `project_map`；
- 两个独立 `exec_command`；
- 同一 Session 并发轮询保护测试。

输出人类可读摘要和机器可读 JSON。

### 9.7 E2 验收

当前自动化验收状态：通过。

- full tests、typecheck、build 和 Loopback benchmark 均通过；
- `session_summary` 集成测试确认 read/search activity 可见；
- 测试确认普通 read 不写入 Durable Audit JSONL；
- Tool callback 输出只统计 UTF-8 字节数，不保留指标专用 payload 副本；
- E2 基准未显示新增观测造成稳定、显著的固定延迟回归；单次结果仍受本机
  调度、文件缓存和子进程启动抖动影响；
- 真实 ChatGPT 与公网 Tunnel 数据必须等待当前服务可重启后补做。

- 能区分 MCP 固定开销和工具执行开销；
- `session_summary` 能看到 read/search 等活动；
- 持久日志不会因普通读取大量增长；
- 基准可以重复运行并比较；
- 埋点不会显著降低请求性能；
- 有真实 ChatGPT 场景前后数据。

---

## 10. E2.5 — Bounded Tool Concurrency Foundation

当前状态：实现与自动化验收完成；真实 ChatGPT 原生并行和公网行为随服务重启统一补做。

当前实现边界：

- 所有注册工具取得全局 Tool Permit；
- shared/heavy read 使用 Workspace 共享锁；
- LocalSpace mutation 使用 Workspace 独占锁；
- Git 写按 Git/Workspace Root Mutex → Workspace 独占锁顺序执行；
- heavy scan 使用独立 Semaphore；
- 同一 Process Session 的交互确定性串行；
- Child Process 同时受全局和 Workspace 进程额度约束，Permit 持有到进程退出；
- `open_workspace(mode=worktree)` 使用全局独占执行锁；
- Activity 指标记录真实 `queuedMs`；
- MCP Server instructions 按当前 Tool Mode 和 Tool Catalog 动态说明 shared read、
  heavy read、mutation、Git write、process 和 process-session 并发边界，并注入
  当前实际并发上限与排队超时；
- 模型侧指南优先推荐 `read_many` 与 `run_checks`，不在每个工具 description
  中重复同一段并发文本；
- 任意 `exec_command` 是否会写共享资源仍不可自动证明，不声明为冲突安全。

### 10.1 目标

在增加 `read_many` 和 `run_checks` 前，为 LocalSpace 建立明确、有界、可审计的并发语义。

并行实现必须同时满足：

- 性能收益可测量；
- 写操作没有丢失更新；
- Git 状态和 Review Checkpoint 语义确定；
- 进程输出不会被错误消费者取走；
- 并发上限不会造成资源耗尽；
- ChatGPT 是否原生并行不会影响核心能力正确性。

### 10.2 当前可行性边界

当前架构已经证明：

- stateless HTTP/MCP 请求能够并发处理；
- 现有测试可同时执行多个 `tools/list`；
- 一个 `ProcessSessionManager` 可以管理多个独立子进程；
- `code_map` 已使用内部 `Promise.all` 并行执行只读子任务。

当前尚未提供：

- 全局工具 Semaphore；
- Workspace 读写锁；
- Git Root 独占锁；
- Process Session Mutex；
- CPU 重型扫描并发限制；
- 每工作区进程上限；
- 明确排队和取消语义。

### 10.3 并发分类

建议定义：

```ts
type ToolConcurrencyClass =
  | "shared-read"
  | "heavy-read"
  | "workspace-write"
  | "git-write"
  | "process-start"
  | "process-session"
  | "global-exclusive";
```

候选映射：

| 工具 | 并发分类 | 默认语义 |
| --- | --- | --- |
| `read` | `shared-read` | 同一工作区可并行 |
| `grep`、`glob`、`ls` | `shared-read` | 同一工作区可并行 |
| `project_map`、`entrypoints` | `shared-read` | 同一工作区可并行 |
| `symbols`、`imports`、`references`、`code_map` | `heavy-read` | 有界并行 |
| `workspace_info`、`doctor`、`session_summary` | `shared-read` | 可并行 |
| `git_status`、`git_diff`、`git_log` | `shared-read` + Git Root 读 | 无 Git 写时可并行 |
| `apply_patch` | `workspace-write` | 同一工作区独占 |
| `show_changes` | `workspace-write` + `git-write` | Workspace 和 Git Root 独占 |
| `git_add`、`git_commit` | `git-write` | 同一 Git Root 独占 |
| `exec_command` | `process-start` | Open-world 命令；只保证有界启动，不自动保证文件系统无冲突 |
| `write_stdin` | `process-session` | 同一 Session 串行，不同 Session 可并行 |
| worktree 创建 | `git-write` | 同一 Git Root 串行 |

工具分类必须来自可信工具目录，不根据模型输入或描述文本动态推断。

`exec_command` 需要单独对待。命令可能只读，也可能写入源码、构建产物、Coverage、数据库或临时文件。第一阶段不尝试从任意 Shell 文本证明命令的完整读写集合：

- 调度器只限制全局和每工作区进程数量；
- 高风险命令审批保持不变；
- 并行启动多个命令必须由调用方确认它们可以安全共存；
- LocalSpace 不把两个通用命令同时成功启动解释为它们在语义上互不冲突；
- `run_checks` 通过受控输入、结果标记和工作区修订检测提供更明确的并行验证语义。

### 10.4 E2.5-T01：全局并发限制

建议增加配置，最终默认值需由 E2 基准确定。初始候选值：

```text
LOCALSPACE_MAX_CONCURRENT_TOOL_CALLS=8
LOCALSPACE_MAX_CONCURRENT_SCANS=2
LOCALSPACE_MAX_CONCURRENT_PROCESSES=4
LOCALSPACE_MAX_WORKSPACE_PROCESSES=2
```

要求：

- 配置有安全上下限；
- `doctor` 展示最终值；
- 超过上限时排队，不无边界创建任务；
- Server shutdown 能取消等待者；
- 客户端断开时尽可能停止无意义等待。

### 10.5 E2.5-T02：Workspace 读写锁

每个 Workspace 建立异步 Read/Write Lock。

#### Shared Read Lock

多个只读工具可以同时进入。

#### Exclusive Write Lock

以下操作持有独占写锁：

- `apply_patch`；
- 任何后续写文件工具；
- `show_changes`；
- 需要工作区一致快照的 Git 写操作。

写操作期间，读操作也等待。原因是多文件 Patch 当前不是整个工作区级别的原子事务，读取进入可能观察到部分更新状态。

### 10.6 E2.5-T03：锁顺序与死锁预防

任何同时需要多个锁的操作必须使用统一顺序：

```text
Global tool permit
→ Git Root lock
→ Workspace lock
→ Process Session lock
```

规则：

- 不允许在持有后序锁时再回头申请前序锁；
- 不使用隐式锁升级，Shared 转 Exclusive 时应释放后重新按顺序申请；
- 多个 Git Root 或 Workspace 锁按规范化路径排序后申请；
- 排队超时或取消时必须释放已获得的所有锁；
- 测试必须覆盖取消、异常和 Server shutdown 后无遗留锁。

### 10.7 E2.5-T04：Git Root 锁

不同 Workspace ID 可能指向同一 Git checkout，因此仅按 Workspace 锁不足够。

以下操作按真实 Git Root 使用独占锁：

- `git_add`；
- `git_commit`；
- `show_changes`；
- worktree 创建和后续删除；
- 未来 branch、merge、reset 等 Git 写操作。

Git 只读查询可以共享，但必须与 Git 写操作互斥，以避免状态来自不一致时间点。

### 10.8 E2.5-T05：Process Session 锁

锁键：

```text
workspaceId + sessionId
```

同一 Session 的以下操作严格按进入顺序执行：

- poll；
- stdin write；
- resize；
- Ctrl-C；
- 未来 terminate。

不同 Session 互不阻塞。

当前 `write_stdin` 会 drain 输出缓冲，因此不允许两个消费者同时轮询同一 Session。

### 10.9 E2.5-T06：Heavy Read 限制

`symbols`、`imports`、`references` 和 `code_map` 当前包含较多同步 TypeScript 解析。`Promise.all` 不能让主线程 CPU 工作真正并行，过多扫描可能增加 Event Loop 延迟。

第一阶段：

- 全局 heavy-read 并发默认上限 2；
- 对大仓库保留文件和结果上限；
- 记录 Event Loop 和工具耗时；
- 暂不引入 Worker Threads。

只有基准证明主线程解析成为关键瓶颈时，再单独设计 Worker Pool。

### 10.10 E2.5-T07：排队、超时和取消

工具结果和活动指标应能区分：

```ts
{
  queuedMs: number;
  executionMs: number;
  concurrencyClass: ToolConcurrencyClass;
}
```

需要定义：

- Queue timeout；
- Server shutdown cancellation；
- Client abort；
- 等待 Workspace 写锁或 Git Root 锁的错误信息；
- 被取消任务是否写入活动记录和审计。

### 10.11 E2.5-T08：并发测试矩阵

至少覆盖：

#### 应成功并行

- 多个独立 `read`；
- `read` + `grep` + `project_map`；
- 不同 Workspace 的只读工具；
- 两个经过确认互不冲突的独立 `exec_command`；
- 不同 Session 的 `write_stdin`；
- 多个并发 `tools/list`。

#### 应排队或串行

- 同一 Workspace 的两个 `apply_patch`；
- `apply_patch` 与 `read`；
- 同一 Git Root 的 `git_add` 与 `git_commit`；
- 两个 `show_changes`；
- 同一 Session 的两个 `write_stdin`；
- worktree 创建与 Git 写操作。

#### 应验证无状态损坏

- 并发 Patch 不发生丢失更新；
- `show_changes` checkpoint 不跳过或重复错误范围；
- Git Index 不产生遗留锁；
- 进程输出只被一个有序消费者 drain；
- 多锁操作遵守统一取锁顺序且异常后无遗留锁；
- 审批 Token 仍保持一次性和上下文绑定。

### 10.12 E2.5 验收

当前自动化验收状态：通过。

- Semaphore、RW Lock、Keyed Mutex 的 FIFO、超时、取消和释放语义通过；
- Tool Scheduler 的 shared read、exclusive write、heavy read、Git Root、
  Process Session 和 worktree-exclusive 路径通过；
- Process Manager 的全局/Workspace 生命周期额度与 Session 交互锁通过；
- 真实 stateless MCP 验证第二个进程在额度为 1 时排队，并返回非零
  `queuedMs`；
- 真实 stateless MCP 验证两个基于同一旧内容的并发 `apply_patch` 只允许
  一个成功，未出现丢失更新；
- full tests、typecheck、build 和 Loopback benchmark 通过；
- 空载基准未显示稳定、显著的固定延迟回归；
- Tool Catalog 测试验证并发指南只引用当前模式可用工具，并随自定义并发配置
  更新数字；真实 stateless MCP initialize 测试验证客户端能够收到该指南；
- 真实 ChatGPT 是否主动并行发起 Tool Calls 仍待服务可重启后记录。

- 全局、Workspace、Git Root 和 Process Session 并发控制完成；
- 只读工具能够并行；
- 写操作和 Git 写操作保持确定性；
- 有资源上限和排队指标；
- 并发压力测试不出现数据损坏；
- 真实 ChatGPT 原生并行行为完成记录；
- 即使 ChatGPT 不原生并行，后续批量工具仍可提供确定性能提升。

---

## 11. E3 — Bounded Batch File Reads

当前状态：实现与自动化验收完成；真实 ChatGPT Tool 选择验收随服务重启统一补做。

当前实现边界：

- `read_many` 仅加入 `codex` 和默认 `hybrid` Tool Surface；
- 每次 1–20 个已知文件，输入顺序稳定；
- 内部并发固定上限 4，整个 Tool Call 仍受 E2.5 Workspace 共享读锁保护；
- 单文件失败不影响其他文件；
- 总文本默认 50,000 字符，最大 200,000；单文件最多返回 50,000 字符；
- 图片作为单文件失败并提示使用 `read`；
- 所有路径预先按 `resolveReadPath` 检查，Skill 不允许在同一批次中通过读取
  `SKILL.md` 后立即解锁其他支持文件；
- 历史 v1.0.6 工具面基线保持不变，v1.1 新增项单独记录。

### 11.1 目标

将多个已知文件的串行读取缩短为一次 MCP 调用，并在 LocalSpace 内部提供有界并行。

### 11.2 输入设计

建议：

```ts
{
  workspaceId: string;
  files: Array<{
    path: string;
    offset?: number;
    limit?: number;
  }>;
  maxTotalCharacters?: number;
}
```

文件数、单文件输出和总输出均必须有界。

### 11.3 返回设计

一个文件失败不应导致全部读取失败：

```ts
{
  results: Array<{
    path: string;
    success: boolean;
    text?: string;
    error?: string;
    truncated?: boolean;
    lineCount?: number;
  }>;
  summary: {
    requested: number;
    succeeded: number;
    failed: number;
    truncated: number;
  };
}
```

### 11.4 安全边界

必须复用：

- `resolveReadPath`；
- allowed roots；
- Skill 读取授权；
- 嵌套指令规则；
- 路径规范化；
- Activity 记录。

批量读取不能绕过先读 `SKILL.md` 后读取 Skill 附属文件的规则。

### 11.5 内部并行

建议：

- 整个调用持有 Workspace shared-read lock；
- 默认单次内部并发 4；
- 使用有界 `mapWithConcurrency`，不直接无边界 `Promise.all`；
- 返回顺序与输入顺序一致；
- 使用部分成功语义；
- 总字符预算优先保证每个文件至少返回错误或摘要。

### 11.6 Skill 更新

`localspace-code-navigation` 应指导：

- 已知多个具体文件时使用 `read_many`；
- 未知文件时先使用搜索和符号工具；
- 不用 `read_many` 替代无边界仓库扫描。

### 11.7 E3 验收

当前自动化验收状态：通过。

- 单元测试覆盖有界并发、输入顺序、部分失败、总字符预算和图片拒绝；
- Tool Catalog、生成式工具面、输出 Schema 和真实 stateless MCP 合同通过；
- 真实 MCP 场景验证 3 个文件中 2 成功、1 失败仍返回有序完整摘要；
- full tests、typecheck 和 build 通过；
- 2026-07-21 本机 Loopback 样本中，3 次串行 `read` 为 `103.44ms`，
  单次 `read_many` 为 `33.83ms`，约减少 67% 客户端往返耗时；
- 样本只覆盖本机 stateless MCP，不包含 ChatGPT 模型、公网 Tunnel 和 UI；
- 真实 ChatGPT 是否主动在合适场景选择 `read_many` 仍待统一重启后验证。

- 读取 3–8 个已知文件只需一次工具调用；
- 与多次单独 `read` 的内容一致；
- 结果顺序稳定；
- 部分错误正常返回；
- Skill 和嵌套指令边界未被绕过；
- 输出上限和截断行为正确；
- 真实 ChatGPT 能在合适任务中选择该工具。

---

## 12. E4 — Bounded Package Check Runner

当前状态：实现与自动化验证完成；真实 ChatGPT 验收等待统一重启；E5 next。

当前实现边界：

- `run_checks` 仅加入 `codex` 和默认 `hybrid` Tool Surface；
- 只接受当前 Workspace `package.json` 中存在的 1–8 个脚本名；
- 包管理器从 `packageManager` 或 lockfile 识别，默认 npm；
- 默认并发 2，最大 4，所有子进程继续受 E2.5 全局/Workspace 生命周期额度约束；
- `failFast` 只阻止尚未启动的 Check，已运行 Check 正常收尾；
- 危险脚本按实际 script 内容分析，并以原子批量审批处理；
- 长任务使用负数 Check Group Session ID，通过现有 `write_stdin` 轮询或 Ctrl-C；
- Check Group 不接受任意 stdin 或 PTY resize；
- 返回每项输出/状态、聚合 Summary、排队时间和 Workspace revision 前后值；
- Validation Summary 同时识别 `exec_command` 和 `run_checks` Durable Audit 事件；
- 不提供任意命令批量器。

### 12.1 目标

减少多项独立验证的启动和轮询往返，同时保持命令安全和结果可追溯。

### 12.2 不采用通用任意命令批量器

第一版不提供：

```ts
run_checks({ commands: ["任意命令", "任意命令"] })
```

这会形成并行任意 Shell，扩大安全面。

### 12.3 推荐输入

第一版仅运行项目声明的 package scripts：

```ts
{
  workspaceId: string;
  checks: string[];
  concurrency?: number;
  failFast?: boolean;
}
```

典型检查名称：

- `typecheck`；
- `lint`；
- `test`；
- `build`。

自定义命令继续使用 `exec_command`。

### 12.4 进程组设计

抽象 Process Group Session，同时复用 `write_stdin`：

```text
run_checks
→ sessionId
→ write_stdin poll/cancel
```

不额外增加多个轮询工具。

### 12.5 并发和资源控制

候选默认：

- `concurrency = 2`；
- 最大值 4；
- 每工作区进程上限受 E2.5 控制；
- 默认不 fail-fast；
- 每项检查独立状态、退出码和输出预算；
- Ctrl-C 终止整个检查组。

Package scripts 仍可能写入 `dist`、Coverage、缓存、数据库或临时目录，因此 `run_checks` 不能将并发验证描述为天然无冲突。第一版要求：

- 仅并行运行调用方明确选择的检查；
- 文档列出常见共享输出目录风险；
- 同名检查不得重复并发；
- 可以为项目策略增加禁止并行的脚本列表；
- 并发失败时保留每项独立输出，不自动用串行重试掩盖问题。

### 12.6 命令安全

即使脚本来自 `package.json`，仍必须：

- 解析实际脚本；
- 执行 command safety 分析；
- danger 级别仍要求 approval；
- 不因脚本名称是 `test` 就自动信任；
- 记录每个子命令风险和审批状态。

### 12.7 工作区修订检测

检查组开始和结束时记录工作区修订摘要。如果运行期间工作区发生变化，结果必须注明：

```text
检查命令成功，但结果可能基于较早的工作区状态。
```

不能将该结果直接描述为当前工作区最终验证通过。

### 12.8 返回结构

候选：

```ts
{
  running: boolean;
  sessionId?: number;
  checks: Array<{
    name: string;
    status: "queued" | "running" | "passed" | "failed" | "cancelled";
    exitCode?: number;
    wallTimeMs?: number;
    outputTruncated?: boolean;
  }>;
  workspaceChangedDuringRun: boolean;
}
```

### 12.9 E4 验收

- 多项独立检查有界并行；
- `write_stdin` 可轮询和取消检查组；
- 每项结果独立；
- 高风险脚本不绕过审批；
- 工作区变化能够识别；
- 相比串行 `exec_command` 明显减少 MCP 往返；
- 不破坏现有单进程 Session。

自动化验收结果（2026-07-21）：

- focused approval、package-check、Check Group、Validation Summary、Tool Surface、输出 Schema 和真实 stateless MCP 测试通过；
- `npm test`、`npm run typecheck` 和 `npm run build` 通过；
- 真实 stateless MCP 验证了并行成功、长检查组轮询、Ctrl-C 边界和危险脚本审批；
- 两个基于同一 Workspace 的独立 package scripts 能通过 `run_checks(concurrency=2)` 并行；
- 精确提交 `1ff5e82` 的正式本机 Loopback 基准中，两个各包含 1 秒任务的 package scripts：两次串行 `exec_command` 为 `3561.49ms`，一次 `run_checks` 为 `2661.87ms`，客户端观察耗时减少约 `25.3%`；
- 完整样本保存在 `docs/baselines/v1.1-e4-run-checks-benchmark.json`；
- Workspace revision 会覆盖 HEAD、暂存/未暂存 Diff 和未跟踪文件内容；被 Git 忽略的缓存或构建产物不纳入该修订摘要；
- 该数字是单机单次样本，不代表公开网络、其他操作系统或真实项目的固定性能承诺；
- 真实 ChatGPT 是否主动选择 `run_checks` 仍待以后统一重启服务后验收。

---

## 13. E5 — Workflow Skills

当前状态：实现与自动化验证完成；真实 ChatGPT Skill 选择验收等待统一重启；E6 next。

当前实现边界：

- 新增 `localspace-debugging`、`localspace-code-review` 和 `localspace-refactoring`；
- 三个 Skill 只提供渐进披露工作流，不增加 Tool、后台 Agent 或隐藏执行循环；
- debugging 聚焦复现、根因、回归测试和修复证据；
- code-review 聚焦 findings-first、兼容性、安全、并发、测试与 unsupported claims；
- refactoring 聚焦行为不变量、调用方迁移、兼容层和死代码清理；
- 继续复用 `read_many`、`run_checks`、导航、Git 和 Patch 工具；
- 框架升级、云部署、数据库迁移和前端设计仍由项目或用户 Skill 提供。

### 13.1 目标

通过渐进披露完善任务方法论，不通过增加大量只读工作流工具来填补指导空白。

### 13.2 新增 Skill

#### `localspace-debugging`

- 复现；
- 缩小范围；
- 搜索错误路径；
- 检查最近提交；
- 最小失败测试；
- 修复根因；
- focused/full validation；
- 回归风险检查。

#### `localspace-code-review`

- correctness；
- compatibility；
- security；
- error handling；
- concurrency；
- tests；
- docs；
- unsupported claims；
- Git Diff review。

#### `localspace-refactoring`

- symbols；
- references；
- imports；
- 公共 API 影响；
- 分阶段迁移；
- 兼容层；
- 删除废弃代码；
- focused/full validation。

### 13.3 暂不内置

以下继续由项目或用户 Skill 提供：

- 特定框架升级；
- 特定云平台部署；
- 特定前端设计；
- 数据库迁移；
- 移动端开发；
- 视频和图像工作流。

### 13.4 E5 验收

- Skill 内容短且触发条件明确；
- 不重复现有内置 Skill；
- `open_workspace` 描述足够让模型正确选择；
- 真实任务按 Skill 工作；
- 不依赖新增默认 workflow helper 工具。

自动化验收结果（2026-07-21）：

- `src/skills.test.ts` 验证九个内置 Skill 均被发现，且三个新增 Skill 的触发描述明确并互不重复；
- 真实 stateless MCP 集成测试验证 `open_workspace` 会广告三个新增 Skill；
- `npm pack --dry-run --json` 验证三个 `SKILL.md` 均进入发布包；
- `npm test`、`npm run typecheck` 和 `npm run build` 通过；
- Tool Surface 合同保持不变，没有新增默认 workflow helper 工具；
- 真实 ChatGPT 是否在调试、审查和重构任务中正确主动选择对应 Skill，仍待以后统一重启服务后验收。

---

## 14. E6 — Optional TypeScript/JavaScript Code Intelligence

当前状态：实现与自动化验证完成；默认关闭；真实 ChatGPT Tool Pack 选择验收等待统一重启；E7 evaluation next。

当前实现边界：

- `LOCALSPACE_TOOL_PACKS=code-intelligence` 可选启用；未设置时核心 Tool Surface 完全不变；
- 为所有 Tool Mode 增加只读 `diagnostics`、`definition`、`implementations` 和 `rename_preview`；
- 第一阶段只支持 `.ts`、`.tsx`、`.mts`、`.cts`、`.js`、`.jsx`、`.mjs` 和 `.cjs`；
- TypeScript 作为 optional dependency 惰性加载；缺失时仅 Pack 返回 `supported: false`，不阻止核心服务启动；
- 识别最近的 `tsconfig.json` / `jsconfig.json`、路径别名和 Workspace 内 project references；
- project references 在交互式 Language Service 中合并 Workspace 内源码，以支持未执行 `tsc -b` 时的源码导航；该语义不替代 `tsc -b` 的精确构建行为；
- Workspace 级缓存复用 Language Service；文件版本变化后增量失效，配置或根文件列表变化后重建；默认最多保留 32 个项目并按 LRU 淘汰，服务关闭时全部释放；
- 配置项目默认最多 5,000 个源码文件；diagnostics 默认 100、最大 500；位置默认 200、最大 1,000；
- 定义、实现和 rename edit 只返回 Workspace 内位置，外部位置计入 `omittedExternal`；
- `rename_preview` 只返回预览，不直接修改任何文件。

### 14.1 目标

弥补轻量 AST 扫描在大型重构上的不足。

### 14.2 工具包边界

候选配置：

```text
LOCALSPACE_TOOL_PACKS=code-intelligence
```

第一阶段只支持 TypeScript/JavaScript，不声明其他语言受支持。

### 14.3 候选能力

- `diagnostics`；
- `definition`；
- `implementations`；
- `rename_preview`。

`rename_preview` 只生成修改预览，不直接写文件。实际修改继续通过 `apply_patch` 或后续明确审批流程。

### 14.4 架构要求

- Workspace 级语言服务缓存；
- 文件修改后增量失效；
- Workspace 关闭或进程退出时释放；
- 大项目有内存和文件边界；
- 缺少 TypeScript 配置时返回清晰状态；
- 默认关闭时不增加核心工具 Schema。

### 14.5 E6 验收

- 正确识别 tsconfig 和 project references；
- 路径别名解析正确；
- diagnostics 与 `tsc` 核心结果一致；
- definition 和 implementation 有可靠测试；
- rename preview 不直接修改文件；
- 不支持语言明确返回 unsupported；
- 关闭工具包后核心工具面不变化。

自动化验收结果（2026-07-21）：

- `src/code-intelligence.test.ts` 验证配置识别、路径别名、文件缓存失效、项目级配置错误、无配置状态、optional dependency 缺失、project-reference metadata 和跨项目源码 definition；
- diagnostics 与 TypeScript Language Service 的语法、语义、建议和配置诊断结果对齐，fixture 中稳定识别 `TS2322`；
- definition、interface implementation 和跨文件 rename preview 有 focused 测试；rename 前后文件内容保持完全一致；
- 非 TypeScript/JavaScript 文件返回 `supported: false`；
- Tool Catalog 合同验证未启用 Pack 时 v1.0.6 + E3/E4 默认工具面不变，启用时每个 Mode 只增加四个批准工具；
- 真实 stateless MCP 集成测试验证四个 Tool 的 Schema、结果和只读边界；
- `npm test`、`npm run typecheck`、`npm run build` 和默认 `npm run benchmark:mcp` 通过；
- 最终 `npm pack --dry-run --json` 验证发布包包含 `dist/code-intelligence.js`，TypeScript 位于 optional dependencies；
- 第一次全量运行曾因 Windows 子进程调度导致既有 process-session 时序断言瞬时失败；单测立即复跑和第二次完整门禁均通过，未发现 E6 生产回归；
- 真实 ChatGPT 是否在 Pack 启用后主动选择新工具，仍待以后统一重启服务后验收。

---

## 15. E7 — Browser Debugging Evaluation

Status: evaluation complete; implementation intentionally not entered.

### 15.1 目标

先评估方案，不直接承诺 LocalSpace 内置浏览器能力。

### 15.2 候选方案

#### A. 独立 Playwright MCP

优点：成熟、避免重复实现、浏览器生命周期独立。

缺点：需要第二个 MCP，Workspace 和浏览器状态分离。

#### B. LocalSpace Browser Pack

候选能力：

- `browser_open`；
- `browser_snapshot`；
- `browser_click`；
- `browser_fill`；
- `browser_screenshot`；
- `browser_console`。

优点：开发闭环统一。

缺点：依赖、攻击面、浏览器生命周期和工具数量明显扩大。

#### C. Skill + `exec_command`

优点：不增加核心代码，现阶段可用。

缺点：结构化输出、页面状态和截图反馈较弱。

### 15.3 决策标准

只有真实前端任务证明方案 C 明显不足时，才实施 Browser Pack。

### 15.4 安全边界

若实施内置浏览器：

- 默认只允许 localhost；
- 外部 origin 需 allowlist；
- 文件上传需显式路径授权；
- 下载使用受控临时目录；
- 不读取用户日常浏览器 Profile；
- 默认独立浏览器上下文；
- 不自动复用个人登录状态。

### 15.5 E7 决策

2026-07-21 完成评估，正式决策如下：

- 日常前端验证采用 Playwright CLI + 渐进披露 Skill；
- 需要持久浏览器状态和富页面结构循环时，可独立接入 Playwright MCP；
- Console、Network、Lighthouse、Performance、Heap 等 Chrome 专项调试采用外部 Chrome DevTools CLI/MCP；
- 当前路线不实现 LocalSpace Browser Pack；
- E7 不新增依赖、不修改 Tool Catalog，也不改变默认 Tool Surface；
- 在 Browser Workflow Skill 实施前，不能将浏览器自动化描述为 LocalSpace supported capability。

完整证据、方案评分、安全边界和重新评估门槛见
[`v1.1-e7-browser-evaluation.md`](v1.1-e7-browser-evaluation.md)。

Browser Pack 只有在至少三个真实前端任务证明 CLI + Skill 明显不足、外部 MCP 仍无法弥补，并且 origin、Profile、上传、下载、remote debugging 和生命周期策略均已冻结时，才允许重新进入设计。

---

## 16. E8 — Policy and Deterministic Automation

Status: workspace policy and deterministic automation complete under automated
validation; real ChatGPT acceptance remains pending until a later service restart.

### 16.1 项目策略

候选文件：

```text
.localspace/policy.json
```

可支持：

- 只读目录；
- 禁止命令模式；
- 允许的 package scripts；
- 强制审批工具；
- 最大批量读取数量；
- PTY 是否允许；
- Browser Pack 是否允许。

策略只能收紧服务器权限。

### 16.2 Hooks

第一阶段不支持任意 Shell Hook。优先内部确定性规则：

- 修改后建议格式化；
- commit 前检查 validation 状态；
- 敏感文件变化强制提示；
- package metadata 修改后建议 package dry-run。

### 16.3 E8 验收

- 项目策略不能扩大服务器权限；
- 恶意仓库不能修改自身信任等级；
- 解析失败时采用更严格规则；
- Hook 不能绕过 command approval；
- 策略决策有活动或审计记录。

### 16.4 Workspace Policy 实施边界

第一条纵向切片采用 `.localspace/policy.json` version 1，支持：

- `readOnlyPaths`；
- `deniedCommandPatterns`；
- `allowedPackageScripts`；
- `maxReadManyFiles`；
- `allowCommands`；
- `allowPty`；
- `requireApprovalTools`。

策略文件只允许收紧权限。首次有效策略会在 LocalSpace state directory
建立持久锚点；后续文件只能与锚点进行单调收紧合并，删除或放宽项目文件
不会自动放宽已接受权限。解析失败、未知字段和非法路径会使 mutation 与
command fail closed，但保留 read-only inspection。

`readOnlyPaths` 约束专用文件与 Git mutation 工具；它不能静态证明任意
shell command 的副作用。需要阻止 shell 写入的项目必须设置
`allowCommands: false`，或仅使用受 allowlist 约束的 `run_checks`。

策略文件自身进入 sensitive-path protection，不能通过 LocalSpace 修改或
stage。策略加载和阻断结果进入 Audit；`open_workspace` 返回结构化 policy
状态与诊断。

Workspace Policy 自动化验收结果（2026-07-21）：

- `src/workspace-policy.test.ts` 覆盖 absent、active、anchored、invalid、
  Windows 大小写、并发首次加载、策略加严持久化、删除/放宽不生效、非法
  path、command deny、script allowlist、PTY、read-many limit 和强制审批；
- `.localspace/policy.json` symlink/非法结构/未知字段采用 fail-closed；
- `write`、`edit`、`apply_patch`、`git_add`、`git_commit`、`read_many`、
  `exec_command`、`run_checks` 和 legacy `shell` 均接入策略检查；
- `git_commit` 会解析全部 staged absolute paths，拒绝 Workspace 外文件并
  重新执行 sensitive-path 与 read-only policy 检查；rename 的 source 与
  destination 均进入检查；
- `git_add` 使用 literal pathspec 且只接受显式文件路径，拒绝可能隐式包含
  protected descendant 的目录 stage；
- `run_checks` 会把 `pre<name>`、主脚本和 `post<name>` 一并纳入危险命令
  分析与 package-script allowlist，生命周期脚本不能绕过审批；
- 真实 stateless MCP 集成测试验证结构化 policy 状态、策略阻断、命令与
  check-group one-time approval、文件保持不变和 Audit `policy_block:*` 事件；
- `npm test`、`npm run typecheck`、`npm run build` 和 focused policy/MCP tests
  通过；
- `npm pack --dry-run --json` 验证发布包包含 `dist/workspace-policy.js` 和
  对应配置、安全文档；
- 默认 Tool Catalog 与 Tool Surface 未增加工具。

### 16.5 Deterministic Automation 实施边界

确定性自动化只执行 LocalSpace 内部、无副作用的规则判断，不支持项目提供的
任意 Shell Hook，也不会自动运行 formatter、test、package、deploy 或 release。

当前规则覆盖：

- source/build config 变化后的格式化建议；
- source/package 变化后的 validation freshness；
- package metadata/lockfile 变化后的 package dry-run freshness；
- env、credential、token、private-key、auth 和 policy-like path 复核；
- staged diff 复核。

`exec_command` 与 `run_checks` 的成功事件写入安全的 validation kind Audit
metadata，因此关闭 command preview 时仍能判断 `test`、`typecheck`、`build`、
`lint`、`smoke` 和 `package`，但不记录完整命令来替代现有日志配置。

`git_commit` 在存在 required recommendation 时使用现有 one-time approval
机制阻断。Token 绑定 exact message、Workspace revision、全部 staged source/
destination paths、required recommendation IDs 和 freshness；任何 Workspace
内容变化都会导致 mismatch。显式 approval 只代表用户接受风险，不会伪造
validation passed。

删除文件、时间戳不可用、服务重启或当前进程缺少 validation Audit evidence
时采用 `unknown`，要求重新验证或显式审批。纯文档变更通常不要求 validation
approval。

Deterministic Automation 自动化验收结果（2026-07-21）：

- `src/deterministic-automation.test.ts` 覆盖 source/package/sensitive path
  分类、validation/package freshness、content-revision 绑定、staged review 和
  approval context；
- `review_checklist`、`task_summary` 与 `validation_summary` 返回统一的
  `automation` 结构，并把必需复核纳入 warnings；
- `git_commit` 在 required recommendation 未满足时返回一次性审批 Token，
  Token 绑定 exact message、content/workspace revision、全部 staged source/
  destination paths 和 required recommendation IDs；
- `exec_command` 与 `run_checks` 的同步及异步完成路径都会记录安全的
  validation action 与当前 Workspace content revision；
- 可识别的直接 shell/package lifecycle `git commit` 被阻断，必须使用专用
  `git_commit`，阻断进入 Activity 与 Durable Audit；
- 真实 stateless MCP 集成测试验证无验证阻断、staged 状态变化导致 Token
  mismatch、精确重试提交、docs-only 提交、自定义异步 check 名称和直接 Git
  commit 旁路阻断；
- `npm test`、`npm run typecheck`、`npm run build` 通过；
- `npm pack --dry-run --json` 验证发布包包含
  `dist/deterministic-automation.js`；
- 未增加 Tool Catalog 或默认 Tool Surface。

### 16.6 Native Runtime Dependency Hardening

Status: implementation and automated release-level validation complete; real
ChatGPT acceptance remains pending until the later coordinated service restart.

LocalSpace 曾直接依赖聚合包 `@earendil-works/pi-coding-agent`，但只使用其中的
少量文件、搜索、Shell、Skill 和项目上下文辅助能力。该聚合依赖同时带入了
未使用的 Pi Agent Core、Pi AI、TUI、模型 Provider 和 Google AI 依赖树。

本阶段改为 LocalSpace 原生实现：

- `read`、`write`、`edit`、`grep`、`glob`、`ls` 和 legacy `shell`；
- Skill 发现、frontmatter、同名碰撞和渐进读取授权；
- `AGENTS.md` / `CLAUDE.md` 初始发现与 Symlink realpath 防护；
- CLI Shell 诊断，复用 `process-platform.ts` 的跨平台 Shell 解析；
- 文件 mutation Keyed Mutex、搜索/读取/进程输出上限和 Workspace containment。

兼容性约束：

- 不改变 Tool Catalog、Tool Surface 或 MCP Schema；
- 默认搜索在 Git Workspace 中通过 `git ls-files` 遵守 ignore 规则；
- 非 Git fallback 使用有界目录遍历、标准跳过目录和根 `.gitignore`；
- legacy `.pi/skills` 只有在用户显式配置时继续作为普通 Skill 路径支持，
  不再代表运行时依赖；
- 图片读取保留 jpg/png/gif/webp MCP attachment，但不加载 Pi 图像/TUI 运行时；
- 当前运行中的旧服务不重启，真实 ChatGPT 验收与其他 v1.1 能力一起延期。

依赖闭包验收目标：

- package/source/lock 中不存在 `@earendil-works/pi-coding-agent`、
  `@earendil-works/pi-agent-core`、`@earendil-works/pi-ai`、`@google/genai`
  和 `protobufjs`；
- 删除 `src/pi-tools.ts`，发布包只包含 `dist/local-tools.js`；
- npm audit 中 Pi 链路的 moderate/high advisories 清零；
- 全量 test、typecheck、build、benchmark、pack 和冷安装通过。

自动化验收结果（2026-07-21）：

- `npm test`、`npm run typecheck`、`npm run build` 和
  `npm run benchmark:mcp` 通过；
- `npm run validate:package` 从当前源码生成 tarball，在独立临时目录执行
  冷安装，验证 CLI 版本、`dist/local-tools.js`、缺失的 `dist/pi-tools.js`
  以及缺失的 Pi/Google 依赖目录；
- `src/runtime-dependencies.test.ts` 锁定 package/source/lock 依赖边界；
- 原生工具测试覆盖读取分页、BOM/CRLF 精确编辑、同文本歧义拒绝、
  gitignore 搜索、图片魔数、Symlink 越界拒绝和 Shell 大输出截断；
- Skill/Workspace 测试覆盖同名碰撞、显式 legacy Skill 路径、渐进读取、
  上下文顺序、managed worktree 和 Symlink realpath 防护；
- 真实 stateless MCP Tool Surface 与输出 Schema 回归通过；
- npm audit 依赖总数由移除前的 `421` 降至 `281`，漏洞由
  `1 low + 5 moderate + 1 high` 降至仅 `1 low`；剩余 low 为 Express
  传递依赖 `body-parser`，与 Pi/Google 链路无关；
- package-lock packages（含根包）为 `282`，不存在 Pi Agent Core、Pi AI、
  Google GenAI、protobufjs 或 Pi 链路中的 high advisory。

---

## 17. E9 — Subagent Research

### 17.1 当前状态

暂缓进入核心开发。

### 17.2 原因

- LocalSpace 定位不是本地自治代理；
- ChatGPT 已负责推理和任务编排；
- 子代理带来工作区冲突和权限复杂性；
- 当前更明确的瓶颈是串行往返和状态并发控制。

### 17.3 未来可能研究的受限角色

- read-only explorer；
- read-only reviewer；
- test-result summarizer。

第一阶段不允许：

- 多代理同时修改同一 checkout；
- 子代理自行 push、publish 或 deploy；
- 子代理扩大权限；
- 隐藏子代理执行历史。

---

## 18. 不采用的并行设计

### 18.1 不提供通用 `parallel_tools`

不计划增加：

```ts
parallel_tools({
  calls: [
    { tool: "apply_patch", args: {} },
    { tool: "git_commit", args: {} }
  ]
})
```

原因：

- Schema 复杂；
- 容易模糊审批边界；
- 工具依赖难以推导；
- 写冲突恢复困难；
- 用户界面难以清晰展示；
- 可能绕过单工具的明确安全语义。

### 18.2 不依赖 ChatGPT 原生并行作为唯一方案

ChatGPT 或其他主机可能在一轮中并行调用独立 MCP 工具，但具体行为不由 LocalSpace 控制。

因此：

- 原生并行可作为额外加速；
- `read_many` 和 `run_checks` 提供 LocalSpace 可保证的批量收益；
- 服务端并发控制保证无论客户端是否并行，状态都正确。

---

## 19. 建议发布边界

### 19.1 `v1.0.7`

建议包含：

- E1 工具契约一致性；
- E2 活动和性能观测；
- E2.5 并发基础设施，或至少完成不改变工具面的锁和上限基础。

候选提交：

```text
test: freeze tool surfaces by mode
refactor: centralize tool catalog metadata
fix: align hybrid instructions with registered tools
docs: align tool mode and workflow documentation
feat: track bounded in-memory tool activity
feat: add MCP request performance metrics
feat: add bounded tool concurrency controls
test: cover workspace git and process concurrency
chore: bump version to 1.0.7
```

### 19.2 `v1.1.0`

建议包含：

- E3 `read_many`；
- E4 `run_checks`；
- E5 新工作流 Skills。

候选提交：

```text
feat: add bounded batch file reads
test: cover batch read safety and partial results
refactor: generalize process session lifecycle
feat: add bounded package check runner
test: cover concurrent checks and approvals
feat: add debugging review and refactoring skills
docs: document efficient ChatGPT coding workflows
chore: bump version to 1.1.0
```

### 19.3 `v1.2.0`

建议包含 E6 可选 TypeScript/JavaScript Code Intelligence。

候选提交：

```text
feat: establish optional code intelligence pack
feat: add TypeScript diagnostics
feat: add definition and implementation lookup
feat: add safe rename preview
docs: document supported language boundaries
chore: bump version to 1.2.0
```

每个提交必须：

- 功能边界单一；
- 自动化测试通过；
- 不夹带无关重构；
- 不提前修改版本号；
- 未经用户明确授权不 push、tag、publish 或创建 Release。

---

## 20. 真实 ChatGPT/MCP 验收矩阵

### 20.1 `v1.0.7`

- 新对话连接 LocalSpace；
- 旧对话在服务重启后继续调用；
- Tool Search 能准确找到工具；
- `hybrid` instructions 不引导调用不存在工具；
- `session_summary` 能看到只读活动；
- 多个独立只读调用可并发处理；
- 同一 Workspace 写操作不会并发损坏；
- 同一 Session 并发轮询被正确串行；
- widgets 保持默认 `changes`。

### 20.2 `v1.1.0`

- ChatGPT 在适当任务中选择 `read_many`；
- 批量读取部分错误不导致整体失败；
- ChatGPT 能通过 `run_checks` 启动多项验证；
- 长检查可通过 `write_stdin` 轮询；
- 危险 package script 仍要求审批；
- aggregate Diff 正常；
- 默认工具选择没有明显退化；
- 相同任务 MCP 往返次数低于 `v1.0.6` 基线。

### 20.3 `v1.2.0`

- TypeScript diagnostics 正确；
- definition 定位真实定义；
- implementations 定位实现；
- rename preview 不直接写文件；
- 非支持语言不被误报为 supported；
- 关闭工具包后核心工具面不变化。

---

## 21. 风险与缓解

| 风险 | 影响 | 缓解 |
| --- | --- | --- |
| 工具目录重构导致注册回归 | 客户端缺少工具 | 各模式精确合同测试 |
| 指标增加日志噪声 | 磁盘增长和隐私风险 | Activity 与 durable audit 分层 |
| 并发写造成丢失更新 | 代码损坏 | Workspace 独占写锁 |
| 多 Workspace 指向同一 Repo | Git Index 冲突 | 按真实 Git Root 独占锁 |
| 同 Session 多轮询 | 输出被错误 drain | Process Session Mutex |
| AST 扫描过多 | Event Loop 延迟 | Heavy-read Semaphore |
| 并行检查争用资源 | 构建或测试不稳定 | 默认并发 2、资源上限、明确结果 |
| 检查期间代码变化 | 误报验证通过 | Workspace revision 检测 |
| Tool Schema 继续膨胀 | 模型选错工具 | 默认核心有界、工具包可选 |
| 过早声明 supported | 用户误判能力 | 自动化 + 真实客户端验收双门禁 |

---

## 22. 当前正式任务边界

当前任务为：

```text
LocalSpace v1.1 Enhancement Track
E1 — Tool Contract and Documentation Alignment
```

E0 已完成：

- 正式计划文档已提交；
- 四种工具模式和 Widget Overlay 已冻结；
- Loopback 性能基准已生成；
- 自动化与真实 ChatGPT/MCP 验收场景已冻结；
- 全量测试、类型检查和构建已通过。

E1 完成前：

- 不实现 `read_many`；
- 不实现 `run_checks`；
- 不进入 LSP；
- 不增加 Browser Pack；
- 不增加 Subagent；
- 不发布新版本。

E1 下一步：

1. 建立可信的单一工具目录；
2. 修复 hybrid instructions 与实际工具面漂移；
3. 修正文档和内置 Skill；
4. 建立 instructions、注册结果和文档合同测试；
5. 精简重复工具描述。

---

## 23. 实施过程报告要求

每完成一个重要阶段，报告必须包含：

1. 已完成内容；
2. 当前 Git 状态；
3. 未完成任务；
4. 下一窗口可直接复制的交接说明。

交接说明至少包括：

```markdown
# 新窗口交接说明

## 项目
路径：
当前分支 / worktree：
当前 Phase / Task：

## 最新提交
最新 commit：
最近关键 commit：

## 已完成
-

## 当前状态
- 测试结果：
- git status：
- 已知问题：

## 下一步任务
1.

## 重要约束
- 不要误判当前阶段
- 不要声明未完成能力为 supported
- 不要写死本地绝对路径
- 修改前先检查现有架构
```

